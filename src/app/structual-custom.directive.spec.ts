import { StructualCustomDirective } from './structual-custom.directive';

describe('StructualCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new StructualCustomDirective();
    expect(directive).toBeTruthy();
  });
});
